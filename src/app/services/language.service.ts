import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class LanguageService {

  // observable contenant le current language

  constructor() { }

  // get current language value
  
  // change current language

  // get key from lang file
}
